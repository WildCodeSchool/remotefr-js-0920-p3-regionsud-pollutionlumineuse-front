# Base URL of the Node.js / Strapi backend
REACT_APP_URL_API=https://subdomain.domain.com

# URL of a feedback form, generated from a 3rd-party service
# (e.g. Google Forms)
REACT_APP_URL_FORMULAIRE=http://geo.pnrpaca.org/

# OPTIONAL (only in prod): basename for React's routing system,
# relative to the origin. e.g. if you host this app on https://mydoma.in/citer-app,
# this should be set to /citer-app
# REACT_APP_ROUTER_BASENAME=/